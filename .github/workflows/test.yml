name: Git Pull Request Workflow
env:
  NODE_VERSION: "12.x"
  IS_VALID: 1
on:
  workflow_dispatch:
  pull_request:
    branches:
      - master

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        if: ${{ env.IS_VALID == 1 }}
        run: |
          echo "${{ env.NODE_VERSION }}"
    
        
