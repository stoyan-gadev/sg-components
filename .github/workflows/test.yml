name: Git Pull Request Workflow

on:
  workflow_dispatch:
  pull_request:
    branches:
      - master
jobs:
  first-job:
    runs-on: ubuntu-latest
    env:
      ONE: 1
      TWO: two
    steps:
      - name: Test
        if: ${{ env.ONE == 1 }}
        run: |
          echo "${{ env.ONE }}"
          echo "${{ env.TWO }}"
    
        
