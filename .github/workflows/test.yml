name: Git Pull Request Workflow

on:
  workflow_dispatch:
  pull_request:
    branches:
      - master

env:
  ONE: 1
  TWO: two

jobs:
  if: ${{ env.ONE }} == 1
  first-job:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "${{ env.ONE }}"
        echo "${{ env.TWO }}"
